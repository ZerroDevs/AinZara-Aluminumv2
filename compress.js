const sharp = require('sharp'); const fs = require('fs'); const path = require('path'); const imgDir = path.join(__dirname, 'assets', 'images'); const htmlFiles = ['index.html', 'services.html']; const cssFiles = ['assets/css/style.css', 'assets/css/features.css']; async function processDirectory(dir) { const files = fs.readdirSync(dir); for (const file of files) { const fullPath = path.join(dir, file); if (fs.statSync(fullPath).isDirectory()) { await processDirectory(fullPath); } else if (file.match(/\.(jpg|jpeg|png)$/i)) { const parsed = path.parse(fullPath); const webpPath = path.join(parsed.dir, parsed.name + '.webp'); console.log('Converting', file); await sharp(fullPath).webp({ quality: 75 }).toFile(webpPath); fs.unlinkSync(fullPath); } } } async function run() { console.log('Processing images...'); await processDirectory(imgDir); console.log('Updating HTML/CSS references...'); [...htmlFiles, ...cssFiles].forEach(file => { const p = path.join(__dirname, file); if (fs.existsSync(p)) { let content = fs.readFileSync(p, 'utf8'); content = content.replace(/\.(jpg|jpeg|png)/gi, '.webp'); fs.writeFileSync(p, content); console.log('Updated', file); } }); } run();
